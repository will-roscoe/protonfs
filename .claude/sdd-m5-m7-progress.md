# M5–M7 progress ledger (2026-07-16)

Plan: GitHub issues #65–#74 are the task briefs. Parallel worktree subagents per wave, PRs reviewed + merged sequentially by controller.

Dependency graph:
- Wave 1 (parallel): #65 support matrix, #67 migrations, #69 transfer backoff, #70 trash, #71 deinit, #72 macOS CI
- Wave 2 (after 65+67 merged): #66 upgrade cmd; then #73 doctor checks (needs 65+67+66 message contract)
- Wave 3: #68 upgrade docs (needs 65–67); #74 v1.0.0 checklist + manual tag (last)

Known merge-conflict hotspots: tests/test_cli_surface.py, docs/stability.rst, src/protonfs/cli.py (touched by #66/#70/#71), src/protonfs/drive.py (#65/#69).

## Status
Wave 1 dispatched 2026-07-16 (6 parallel sonnet agents, worktree-isolated).
- [x] #65 M5.1 support matrix — PR #75 merged (reviewed inline, CI green). API: `SUPPORTED_DRIVE_VERSIONS`, `highest_supported()`, `is_supported()` in install.py; `DriveClient.drive_version()` in drive.py.
- [x] #67 M5.3 migrations — PR #77 merged. API for #66: `pending_migrations(root)`, `run_migrations(root, dry_run=False) -> MigrationResult`, `layout_version(root)`, `MIGRATIONS`, `CURRENT_LAYOUT_VERSION` in src/protonfs/migrations.py.
- [x] #69 M6.1 transfer backoff — PR #78 merged. Shared `_retry_with_backoff`; `PROTONFS_TRANSFER_*` env knobs.
- [x] #70 M6.2 trash subcommand — PR #80 merged; live-verified trash list + parent_name (2/2 live tests green on real account). codecov/patch soft-red on thin wrappers, accepted.
- [x] #66 M5.2 upgrade — PR #81 merged (implemented inline by controller after spawn denial). Helper for #73: `upstream_ahead_message(upstream, highest)` + `upstream_stable_version(opener=None)` in src/protonfs/commands/upgrade.py.
- [x] #71 M6.3 deinit — PR #79 merged. Lock refusal via repo_lock; lock file deliberately excluded from removal list.
- [x] #72 M6.4 macOS CI — PR #76 merged. macos-latest (arm64) + macos-15-intel jobs green; darwin badges; fixed secretservice tests not pinning platform to Linux.
- [x] #73 M6.5 doctor checks — PR #82 merged (inline by controller). warn level added to Check; shares upstream_ahead_message with upgrade.
- [x] #68 M5.4 upgrade guide — PR #83 merged (inline by controller). docs/upgrading.rst + reference entry + cross-links.
- [x] #74 M7.1 v1.0.0 — DONE. Gate live-passed (after #84 flake fix); fixture migration cycle verified; v1.0.0 on GitHub+PyPI; v1.0.1 confirms post-1.0 patch bumps. M7 milestone closed.
  - INCIDENT: v1.0.0 auto-fired — #85's commit body quoted '+:major', new directive parser matched it. Kept per user decision; #86 requires directives alone on own line.
  - Also shipped: #85 +:<spec> directives + prerelease ladder + generated release notes; #86 own-line fix + curated 1.0.0 changelog; #87 workflow injection/permissions security fixes.
  - NOTE: subagent spawns blocked by classifier (context-mode hook injection); #66/#73/#68/#85/#86/#87 done inline.

## Post-1.0 docs overhaul (in progress, branch docs/docstring-and-guides)
1. Docstrings → Sphinx RST style (:param:/:returns:/:raises:/seealso/examples); interrogate 48.2%→pass 80+ w/o `|| true` in ci.yml badge step; tests excluded (already src-only).
2. docs/reference: in-depth per-subcommand breakdown; "what I want to do"→command guide; full example walkthroughs.
3. README ↔ docs index sync mechanism; badges kept on both; polish.
4. Furo TOC frontend errors: comment out autogenerated `.. contents::` (all 6 rst files); sweep docs build warnings.

## Docs overhaul — DONE (PR #89 merged)
1. ✓ Docstrings 48.2%→98.5% RST style; interrogate gate in CI (fail-under=80, tests excluded).
2. ✓ docs/getting-started/guide.rst task-index + walkthroughs; reference already deep.
3. ✓ Single-source README↔docs: docs/_shared/overview.rst included by index.rst; sync_readme.py generates README block; CI --check gate + 7 tests.
4. ✓ Furo TOC: removed .. contents:: from all 6 pages; homepage badges added.
Also: versioning +:<spec> directives/prerelease ladder/generated notes (#85), own-line directive fix (#86), workflow security (#87). v1.0.0 + v1.0.1 live.

## Backlog (in progress: branch chore/community-standards)
5. GitHub community standards: Code of Conduct, CONTRIBUTING, SECURITY policy, issue templates, PR template. Conform to GitHub community-standards checklist.
